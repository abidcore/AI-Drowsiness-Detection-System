# AI Drowsiness Detection System — Python Dependencies
# Tested with Python 3.12+
# Install with: pip install -r requirements.txt
#
# Notes:
#   - dlib requires CMake and a C++ compiler to build from source on
#     some platforms. If you only intend to use the default MediaPipe
#     backend, dlib is NOT required to run this project -- see
#     "Enabling the dlib Backend" in README.md before installing it.
#   - playsound 1.2.2 is pinned specifically because later 1.3.x
#     releases introduced breaking changes/regressions on some
#     platforms for simple .wav playback.

opencv-python>=4.9.0
mediapipe>=0.10.9
numpy>=1.26.0
scipy>=1.11.0
imutils>=0.5.4
playsound==1.2.2

# Optional -- only required if DETECTION_BACKEND = "dlib" in config/settings.py
dlib>=19.24.0
